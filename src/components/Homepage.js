import { Typography } from "@mui/material";
import React from "react";

function Homepage() {
  return (
    <div
      style={{
        width: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
      }}
    >
      <Typography style={{color:"white", fontSize:"72px"}}>HR and Management system</Typography>
    </div>
  );
}

export default Homepage;
